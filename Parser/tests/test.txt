class MyClass {
	int a[10];
	int findMax(int array[100], int x, int y) {
		for( int idx = 99; idx > 0; idx = idx - 1 ) {
			if(array[idx] > maxValue) then {
				maxValue = array[idx];
			}
			else {};
		};
	};
};

program {
	int x;
	x = randomize(1, 2);
};

float randomize(int a, int b) {
	MyClass var0;
	float var1;
	
};